<!doctype html>
<html lang="en" class="text-base">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}" type="text/css">
    <title>{{ site.title }}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🦆</text></svg>">

    <meta property="og:title" content="{{ site.title }}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ site.url }}" />
    <meta property="og:image" content="{{ site.url + 'assets/images/og.png' | url }}" />
    <meta property="og:description" content="" />

    <style>
      i[data-tech]{width: 100%; height: 100; position: relative; overflow: hidden;}
      {%- for item in tech -%}
      [data-tech="{{ item.name }}"]::after {
        content: " ";
        width: 100%;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        background: {{item.color}};
      }
      {%- endfor -%}
    </style>
  </head>
  <body class="min-h-[100svh] flex flex-col selection:bg-black selection:text-white">

    {% include "partials/header.njk" %}