---
layout: layouts/base
---
<section class="mb-20">
  <div class="text-lg md:text-xl mb-20">
    <p class="">
      Design and development of fully tailored websites that are useful, simple, and effortless to maintain. Currently <span class="text-green-500">available</span> for new commissions, consulting, and development services for other designers and agencies.
    </p>
  </div>
  <ul class="flex flex-col">
    {%- for item in sites -%}
      {% if item.disabled == true or item.url == '' %}
        <li class="grid sm:grid-cols-8 gap-y-1 flex-col justify-between relative py-3 border-b border-gray-400">
          <div class="col-span-2">
            <h2 class="text-gray-400">{{ item.name }}</h2>
          </div>
          <div class="col-span-5">
            <p class="text-gray-400">{{ item.text }}</p>
          </div>
          <div class="col-span-1 text-right hidden sm:block">
            {{ item.year }}
          </div>
        </li>
      {% else %}
        <a class="link | group" href="{{ item.url }}" target="_blank" noopener noreferrer data-frame-info-top="" data-frame-info-btm="
              <div class='flex flex-wrap gap-4'>
                <span class='name text-sm'>{{ item.name }}</span>
                {% for tech in item.tech %}
                  <div class='flex gap-2 items-center'>
                    <div class='w-3 h-3 rounded-full relative overflow-hidden flex text-sm'>
                      <i class='w-full h-full' data-tech='{{ tech }}'></i>
                    </div>
                    <span class='techname opacity-50 text-sm'>{{ tech }}</span>
                  </div>
                {% endfor %}
                </div>
                ">
          <li class="grid sm:grid-cols-8 gap-y-1 flex-col justify-between relative py-3 border-y border-gray-400 -mt-px">
            <div class="col-span-2 mr-2">
              <h2 class="">{{ item.name }}</h2>
            </div>
            <div class="col-span-5">
              <p class="text-gray-400">{{ item.text }}</p>
            </div>
            <div class="col-span-1 text-right hidden sm:block">
            {{ item.year }}
          </div>
          </li>
        </a>
    {% endif %}
    {% endfor %}
  </ul>
</section>